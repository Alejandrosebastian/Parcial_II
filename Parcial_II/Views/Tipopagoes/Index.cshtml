@model IEnumerable<Parcial_II.Models.Tipopago>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre_tipopago)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_tipopago)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipopagoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipopagoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipopagoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
